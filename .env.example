# Caminho do arquivo SQLite usado por src/lib/db.ts para persistir
# usuários, sessões e histórico de gerações (etapa
# backend-persistence-production). Use ':memory:' (padrão) para
# desenvolvimento efêmero ou um caminho de arquivo, ex.: ./data/anki.db
ANKI_DB_PATH=:memory:
